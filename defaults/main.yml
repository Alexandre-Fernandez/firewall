firewall_allowed_input: []

firewall_allowed_output: []

firewall_is_dns_allowed: true

firewall_is_all_ssh_allowed: true

firewall_ssh_port: -1 # used when firewall_is_all_ssh_allowed is false (instead of auto-detect)

